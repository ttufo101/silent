# 遗留问题

## 应用升级链路

- 当前安装包服务器为 `http://106.52.77.108:80`。Android 临时允许该主机使用明文 HTTP，客户端仍校验安装包大小和 SHA-256。HTTP 与同信道返回的 SHA-256 只能发现文件损坏，不能证明安装包来源；后续需要迁移到 HTTPS，并删除 Android 对该主机的明文流量例外。
- 当前 `update_url` 必须直接返回 HTTP 200 和安装包内容，不支持 301、302 或其他重定向。后续如接入对象存储或 CDN，需要逐跳校验协议和可信主机后再允许重定向。

## 诊断日志上传

- 当前诊断日志上传服务为 `http://106.52.77.108:80`。日志在上传前会脱敏并计算 SHA-256，但 HTTP 无法保证传输机密性或服务器身份；后续需要迁移到 HTTPS，并删除 Android 对该主机的明文流量例外。
- 文件服务幂等记录默认只在单实例内存保留 24 小时，不支持多实例共享。客户端会持久化未完成任务，但服务端仍需将幂等记录迁移到共享持久化存储，才能可靠覆盖服务重启和多实例部署。

## 节点配置同步

- 当前刷新节点配置时会先保存新的配置文件和数据库记录，再将配置应用到 Core。如果 Core 应用失败，本地可能已经切换到无法使用的新配置。后续需要改为临时文件校验、Core 预加载成功后再提交本地配置，并在任一步骤失败时继续使用旧配置。

## 设置页信息架构

- 「高级功能」分组已整组取消，`lib/views/config/` 目录（基本配置、进阶配置及其 DNS、网络、按需运行、附加规则、脚本子页）已整体删除。当前版本没有任何导入或编辑自建 YAML 配置的入口；若后续需要服务从 fl_clash 迁移、依赖自建配置的老用户，必须另行提供导入能力。
- 「仅统计代理」开关已移除，但 `onlyStatisticsProxy` 字段仍在模型中并会传给原生层，当前等价于锁定为默认的「统计全部流量」。若确认不再需要该能力，可连带清理模型字段与原生桥接。
- `lib/views/backup_and_restore.dart` 在 `lib/` 内无任何引用，属既有死代码，尚未清理。

## 关于页缺少法律与许可信息

- 关于页（`lib/views/about.dart`）当前只有版本号和「检查更新」，缺少**隐私政策**与**用户协议**入口。订阅制产品上架与合规通常需要这两项，但项目内暂无对应文案与 URL——**需要产品侧提供链接与文案后**再补，不应凭空构造。
- 本应用 fork 自 fl_clash，关于页还应展示**开源许可**信息。需先确认展示形态（应用内许可列表页，还是跳转外链）再实现。

## 连接页定时器未取消

- `ConnectionsView` 内部有定时器在页面销毁后未取消，widget 测试收尾会因此断言失败。属小型资源泄漏，尚未修复。
